/*
 * knight.js
 *
 * This model class represents a knight card whose movement is controlled by the player through dice rolls.
 *
 * Properties:
 * - id: Specifies a unique identifier for the knight card
 */

export class Knight {

    id;

    constructor(id) {
        this.id = id;
    }
}